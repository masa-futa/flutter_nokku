
import 'package:flutter/material.dart';

class Sample1Screen extends StatelessWidget {
  const Sample1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sample1Screen'),
      ),
      body: const Center(
        child: Text('Sample1Screen'),
      ),
    );
  }
}
