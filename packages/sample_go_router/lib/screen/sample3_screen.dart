
import 'package:flutter/material.dart';

class Sample3Screen extends StatelessWidget {
  const Sample3Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sample3Screen'),
      ),
      body: const Center(
        child: Text('Sample3Screen'),
      ),
    );
  }
}
